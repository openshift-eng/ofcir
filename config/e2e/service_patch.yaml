apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: system
spec:
  type: NodePort
  ports:
    - name: https
      nodePort: 30007
      port: 443
      protocol: TCP
      targetPort: 8443